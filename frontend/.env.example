# Kosongkan (jangan isi host tetap) supaya request API pakai path relatif ("/api/v1")
# terhadap host yang dipakai browser mengakses aplikasi ini — otomatis benar baik diakses
# via localhost MAUPUN via IP LAN dari perangkat lain, karena diteruskan lewat proxy Vite
# (lihat vite.config.ts, server.proxy['/api']) ke container backend.
# Isi HANYA kalau backend benar-benar di host/domain lain yang beda dari frontend.
VITE_API_URL=
